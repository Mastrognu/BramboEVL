context Coordinates {
	constraint NoOverlapping {	
		check : forAll(c1, c2 | c1 <> c2 implies not 
			(c1.coordx = c2.coordx and c1.coordy = c2.coordy))

		message : 'There are overlapping coordinates.'
	}
}